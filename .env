# SQLite for local dev — no secrets here, committed on purpose so `npm run dev` works alone.
# Swap to another database (and adapter in prisma.config.ts / lib/db.ts) for deployment.
DATABASE_URL="file:./prisma/dev.db"
